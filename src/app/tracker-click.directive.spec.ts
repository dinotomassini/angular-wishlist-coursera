import { TrackerClickDirective } from './tracker-click.directive';

describe('TrackerClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackerClickDirective();
    expect(directive).toBeTruthy();
  });
});
